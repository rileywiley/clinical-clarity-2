"""Forecast engine — pure Python.

Implementation lands in Phase 1 against PRD §6. Until then this package is a
placeholder so the dependency graph, CI, and uv workspace can be wired end-to-end.

CLAUDE.md golden rule #2: this package must remain free of web/DB/HTTP/ORM/framework
imports. It takes data in and returns results.
"""

__version__ = "0.1.0"
